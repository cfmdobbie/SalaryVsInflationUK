<resources>

    <!-- Base application theme for API 11+ -->
    <style name="AppBaseTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <!-- API 11 theme customizations -->
    </style>

    <!-- Make buttons with the "BorderlessButton" style *actually* borderless! -->
    <style name="BorderlessButtonBaseStyle" parent="android:Widget.Holo.Button.Borderless" />

    <!-- Button bar -->
    <style name="ButtonBarButton" parent="@android:style/Widget.Holo.Button.Borderless">
        <item name="android:textColor">@android:color/primary_text_light</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dip</item>
        </style>

    <style name="ButtonBar" parent="@android:style/Holo.Light.ButtonBar.AlertDialog"/>

</resources>